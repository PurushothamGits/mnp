pipeline{
    agent any
   stages {
        stage('Git Clone') {
            steps {
                checkout scm
            }
        }

        stage("Deployment") {
            steps {
                sh '''
               sudo cp var/lib/jenkins/workspace/pipeline-02/purushotham.html var/www/html
            '''
        }
    }
}
